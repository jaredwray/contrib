html
    head
        title= auction.title
        meta(property="fb:app_id" content=facebookAppId)
        meta(property="og:url" content=auction.link)
        meta(property="og:image:height" content="400")
        meta(property="og:image:width" content="400")
        meta(property="og:type" content='article')
        meta(property="og:title" content=auction.title)
        meta(property="og:description" content=auction.description)
        meta(property="og:image:url" content=auctionImageUrl)
        meta(property="og:image:secure_url" content=auctionImageUrl)
        meta(property="twitter:title" content=auction.title)
        meta(property="twitter:description" content=auction.description)
        meta(property="twitter:image" content=auctionImageUrl)
        meta(property="twitter:url" content=auction.link)
