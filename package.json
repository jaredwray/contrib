{
  "name": "contrib-app",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build-client": "cd ./client && yarn && yarn build",
    "build-server": "cd ./server && yarn && yarn build",
    "test-client": "cd ./client && yarn test",
    "test-server": "cd ./server && yarn test:cov",
    "build": "yarn build-client && yarn build-server"
  },
  "dependencies": {
    "@nestjs/mongoose": "^7.2.2",
    "mongoose": "^5.11.13"
  }
}
