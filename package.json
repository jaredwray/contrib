{
  "name": "contrib-app",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build-client": "yarn --cwd client install && yarn --cwd client build",
    "build-server": "yarn --cwd server install && yarn --cwd server build",
    "test-client": "yarn --cwd client test",
    "test-server": "yarn --cwd server test:cov",
    "build-docker": "docker build --pull --rm -f \"Dockerfile\" -t contrib-app:latest \".\"",
    "run-docker": "docker run --rm -d -p 3000:3000/tcp --env-file ./server/.env contrib-app:latest",
    "build": "yarn build-client && yarn build-server && yarn build-docker",
    "start": "yarn --cwd server start"
  }
}
