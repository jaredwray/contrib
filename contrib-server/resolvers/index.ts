import { UserResolver } from './UserResolver';
import { BookResolver } from './BookResolver';
import { UserAccountResolver } from './UserAccountResolver';

/**
 * @description holds all resolvers
 */

// export const resolvers = [UserResolver, BookResolver, UserAccountResolver];
export const resolvers = [UserAccountResolver];
